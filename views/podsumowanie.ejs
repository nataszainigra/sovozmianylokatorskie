<%- include('partials/header') %>

<!-- SUCCESS MESSAGE -->
<div class="max-w-3xl mx-auto">
  <div class="bg-green-50 border-2 border-green-500 rounded-lg p-8 mb-8 text-center">
    <div class="mb-4">
      <svg class="w-16 h-16 text-green-500 mx-auto" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
    </div>
    <h1 class="text-3xl font-bold text-green-800 mb-2">Dziękujemy!</h1>
    <p class="text-lg text-green-700 mb-4">Wniosek został przesłany do Działu Technicznego</p>
    <p class="text-sm text-green-600">Możesz zamknąć tę stronę</p>
    <p class="text-sm text-green-600 mt-2">Podsumowanie zostało wysłane na podany adres e-mail</p>
  </div>

  <!-- PROCESS INFO -->
  <div class="bg-white border rounded-lg p-6 mb-6">
    <h2 class="text-xl font-semibold mb-4">Dalsze kroki</h2>
    <div class="space-y-4">
      <div class="flex gap-4">
        <div class="flex-shrink-0 w-8 h-8 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center font-bold">1</div>
        <div>
          <h3 class="font-medium text-gray-900 mb-1">Weryfikacja przez Dział Techniczny</h3>
          <p class="text-sm text-gray-600">
            Dział techniczny dokonuje weryfikacji zgłoszonych zmian pod kątem zgodności z przepisami budowlanymi,
            techniczną wykonalnością oraz harmonogramem budowy.
          </p>
        </div>
      </div>

      <div class="flex gap-4">
        <div class="flex-shrink-0 w-8 h-8 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center font-bold">2</div>
        <div>
          <h3 class="font-medium text-gray-900 mb-1">Analiza wniosku</h3>
          <p class="text-sm text-gray-600">
            Analiza przeprowadzana jest w terminie do <strong>10 dni roboczych</strong> od daty otrzymania pełnej dokumentacji.
          </p>
        </div>
      </div>

      <div class="flex gap-4">
        <div class="flex-shrink-0 w-8 h-8 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center font-bold">3</div>
        <div>
          <h3 class="font-medium text-gray-900 mb-1">Rozwiązania alternatywne</h3>
          <p class="text-sm text-gray-600">
            W przypadku konieczności, deweloper może zaproponować alternatywne rozwiązania.
          </p>
        </div>
      </div>

      <div class="flex gap-4">
        <div class="flex-shrink-0 w-8 h-8 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center font-bold">4</div>
        <div>
          <h3 class="font-medium text-gray-900 mb-1">Informacja zwrotna</h3>
          <p class="text-sm text-gray-600">
            Po zakończeniu analizy dział techniczny przekazuje swoje stanowisko do działu sprzedaży,
            który następnie informuje klienta o możliwości lub braku możliwości realizacji zmian.
          </p>
        </div>
      </div>
    </div>
  </div>
</div>

<h2 class="text-xl font-semibold mb-4 mt-8">Szczegóły złożonego wniosku</h2>

<p class="text-sm text-zinc-600 mb-4">
  Poniższe wartości to kalkulacja wstępna wg aktualnego cennika. Elementy bez automatycznej wyceny oznaczono jako
  „Do analizy Działu Technicznego". Finalny kosztorys zostanie wysłany na e-mail po weryfikacji.
</p>

<!-- KARTA DANYCH KLIENTA (pełna szerokość) -->
<section class="p-4 border rounded-md bg-white mb-6">
  <h2 class="font-medium mb-3">Dane kupującego</h2>
  <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-x-8 gap-y-2 text-sm">
    <div><span class="text-zinc-500">Kupujący:</span> <span class="font-medium"><%= request.buyerName %></span></div>
    <div><span class="text-zinc-500">Lokal:</span> <span class="font-medium"><%= request.unitNumber %></span></div>

    <% if (request.email) { %>
      <div><span class="text-zinc-500">E-mail:</span> <span class="font-medium"><%= request.email %></span></div>
    <% } %>

    <% if (request.phone) { %>
      <div><span class="text-zinc-500">Telefon:</span> <span class="font-medium"><%= request.phone %></span></div>
    <% } %>

    <% if (request.addressStreet || request.addressZip || request.addressCity) { %>
      <div class="sm:col-span-2 lg:col-span-3">
        <span class="text-zinc-500">Adres korespondencyjny:</span>
        <span class="font-medium">
          <%= [request.addressStreet, request.addressZip, request.addressCity].filter(Boolean).join(', ') %>
        </span>
      </div>
    <% } %>

    <% if ((request.attachments||[]).length) { %>
      <div class="sm:col-span-2 lg:col-span-3">
        <span class="text-zinc-500">Załączniki:</span>
        <ul class="mt-1 list-disc pl-5 space-y-1">
          <% (request.attachments || []).forEach(a => { %>
            <li class="break-all"><code>/uploads/<%= a %></code></li>
          <% }) %>
        </ul>
      </div>
    <% } %>
  </div>
</section>

<!-- TABELA KOSZTORYSU (pełna szerokość) -->
<section class="p-4 border rounded-md bg-zinc-50">
  <div class="flex items-center justify-between">
    <h2 class="font-medium">Kosztorys wstępny</h2>
  </div>

  <div class="mt-3 overflow-x-auto">
    <table class="w-full text-sm">
      <thead class="text-left text-zinc-600">
        <tr>
          <th class="py-2 pr-2">Pomieszczenie</th>
          <th class="py-2 pr-2">Branża</th>
          <th class="py-2 pr-2">Opis / Kod cennika</th>
          <th class="py-2 pr-2 w-20">Jedn.</th>
          <th class="py-2 pr-2 w-16">Ilość</th>
          <th class="py-2 pr-2 w-28">Status</th>
          <th class="py-2 pr-2 w-28">Cena (netto)</th>
          <th class="py-2 pr-2 w-28 text-right">Razem</th>
        </tr>
      </thead>
      <tbody>
        <% (estimate.rows || []).forEach((r, i) => { 
             const it = (request.items || [])[i] || {}; %>
          <tr class="border-t align-top">
            <!-- Pomieszczenie -->
            <td class="py-2 pr-2"><%= it.room || '—' %></td>

            <!-- Branża -->
            <td class="py-2 pr-2"><%= it.branch || '—' %></td>

            <!-- Opis / Kod -->
            <td class="py-2 pr-2">
              <div><%= r.title %></div>
              <% if (r.code) { %>
                <div class="text-xs text-zinc-500">Kod: <%= r.code %></div>
              <% } else if (it.code) { %>
                <div class="text-xs text-zinc-500">Kod: <%= it.code %></div>
              <% } %>
            </td>

            <!-- Jednostka -->
            <td class="py-2 pr-2"><%= r.unit || '—' %></td>

            <!-- Ilość -->
            <td class="py-2 pr-2"><%= r.qty %></td>

            <!-- Status -->
            <td class="py-2 pr-2 w-28">
              <% if (r.manual) { %>
                <span class="inline-block text-xs rounded px-2 py-1 bg-amber-100 text-amber-700">Do analizy</span>
              <% } else { %>
                <span class="inline-block text-xs rounded px-2 py-1 bg-emerald-100 text-emerald-700">Wycenione</span>
              <% } %>
            </td>

            <!-- Cena jedn. (netto) -->
            <td class="py-2 pr-2"><%= r.unitPrice ? (r.unitPrice.toFixed(2) + ' zł') : '—' %></td>

            <!-- Razem (netto) -->
            <td class="py-2 pr-2 text-right"><%= r.lineTotal ? (r.lineTotal.toFixed(2) + ' zł') : '—' %></td>
          </tr>
        <% }) %>
      </tbody>

      <tfoot>
        <tr class="border-t">
          <td colspan="7" class="py-2 pr-2 text-right font-medium">Suma netto (pozycje wycenione)</td>
          <td class="py-2 pr-2 text-right font-medium"><%= estimate.subtotal.toFixed(2) %> zł</td>
        </tr>
        <tr>
          <td colspan="7" class="py-1 pr-2 text-right">VAT 23%</td>
          <td class="py-1 pr-2 text-right"><%= estimate.vat.toFixed(2) %> zł</td>
        </tr>
        <tr>
          <td colspan="7" class="py-2 pr-2 text-right text-lg font-semibold">Suma brutto</td>
          <td class="py-2 pr-2 text-right text-lg font-semibold"><%= estimate.total.toFixed(2) %> zł</td>
        </tr>
      </tfoot>
    </table>
  </div>
</section>

<%- include('partials/footer') %>
